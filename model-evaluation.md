---
layout: default
title: Model Evaluation
---

## Оглавление
* TOC
{:toc}

Это очень больная тема, которой почему-то уделяют мало внимания.

* Можно получить не валидные результаты
* Можно сделать модель, которая имеет высокие метрики, но падает в проде
* Некоторые статьи некорректные и лучше обходить их стороной
* Можно поставить слишком амбициозный эксперимент: окажется, что данных и ресурсов не хватает, чтобы получить стат значимые результаты. Время потрачено, видеокарты сожгли зря
* Иногда для измерения качества используют языковые модели (LLM as a judge). Насколько этому можно верить?

# Metrics

# Train / validation / test

# Настройки
## Логирование
Это очень важно! Если не вести логи, то можно считать, что исследование пойдёт насмарку. У меня был случай из жизни, когда потратили 10 дней и 100+ тысяч рублей (правда это на две команды, но можно поделить на два), а потом Гасников спросил про стат значимость и ответить было нечего. Никогда так не делайте.

Самый простой способ — логировать в бинарный файл (с раширением `.tfevents`). В каждой записи содержится событие, связывающее имя метрики (tag), ее численное значение (ось Y) и шаг обучения или эпоху (global_step, ось X).

TensorBoard умеет открывать такой файл. Для примера вот.

В VS code нажимаем ctrl+shift+p (если расширение установлено)
<figure>
  <img
    src="{{ '/assets/Screenshot from 2026-08-28 23-31-17.png' | relative_url }}"
  >
</figure>

<figure>
  <img
    src="{{ '/assets/Screenshot from 2026-08-28 23-31-39.png' | relative_url }}"
  >
</figure>

Нам нужно это:

<figure>
  <img
    src="{{ '/assets/Screenshot from 2026-08-28 23-32-03.png' | relative_url }}"
  >
</figure>

В конце всех этих путей лежат бинарники с логами:

<figure>
  <img
    src="{{ '/assets/Screenshot from 2026-08-28 23-32-14.png' | relative_url }}"
  >
</figure>

Выбираем директорию tensorboard_logs и теперь можно посмотреть графики экспериментов.
<figure>
  <img
    src="{{ '/assets/Screenshot from 2026-08-28 23-34-57.png' | relative_url }}"
  >
</figure>

Это было как вариант настройки.

Есть WandB — сервис для отслеживания прогресса обучения. Там есть логи, веса, графики, можно добавлять коллег. Однако на мой вкус он слишком капризный к настройке. В пайплайне обучения стоит хотя бы поставить флаг True/False подключения к wandb, когда в терминале запускаешь модель.

Благодаря логам можно проверить, насколько результат стабилен относительно сида, оценить доверительный интервал, провести стат тесты. Если этого не сделать, то по-хорошему эксперимент некорректный. Допустимо как промежуточный шаг вашей работы, но для защиты результата надо провести запуски, по которым можно будет сделать оценку. GPU дорогие, поэтому лучше не полениться сразу всё настроить.

## Yandex DataSphere

Это смерть и ужас в плане логирования и просмотра графиков, потому что браузер плохо тянет обновление данных (по крайней мере у меня). Возможно, в браузере это реально трудно сделать. Я не придумала ничего лучше: я логирую в директорию, а потом скачиваю zip-архив с ней и смотрю графики локально через TensorBoard в VS Code.

Датасфера не даёт просто так скачивать файлы через кнопочку, вместо этого она предлагает скачать html. Кроме того, там нет обычного терминала (по крайней мере я не нашла), поэтому вместо него приходится использовать ноутбук.

```
!zip -r logs.zip tensorboard_logs
```

```
from IPython.display import FileLink
FileLink('logs.zip')
```

```
import base64
from IPython.display import HTML

def create_download_link(filename):
    with open(filename, 'rb') as f:
        data = f.read()
    b64 = base64.b64encode(data).decode()
    payload = f'data:application/zip;base64,{b64}'
    html = f'''
    <a href="{payload}" download="{filename}" 
       style="padding: 10px; background: #007bff; color: white; text-decoration: none; border-radius: 5px;">
       СКАЧАТЬ АРХИВ LOGS.ZIP
    </a>
    '''
    return HTML(html)

create_download_link('logs.zip')
```

Но оно того стоит, там классные GPU.

# Rules of Machine Learning

# Leakage

# Temporal evaluation
Классическая задача — по прошлому предсказывать будущее. Как это делать? Самый простой и достаточно популярный способ — time split. Однако, если задуматься, то это ведь очень сильный claim. Допустим, мы делим данные в отношении 8:1:1 (train, valid, test): по первым 80% предсказать следующие 20%. Где гарантия, что здесь нет утечки?

Рассмотрим такой пример: пользователь покупает товары. Для модели это эмбеддинги в едином семантическом пространстве. Пусть пользователь покупает белые кроссовки и джинсы, и они семантически взаимосвязаны: например, они одного бренда, сейчас завирусились в тиктоке, являются частью косплей-костюма на аниме персонажа. По-хорошему модель должна выучить эту закономероность.

Однако в нашем кейсе произошло следующее. Первые 80% это только белые кроссовки, сдедующие 10% тоже кроссовки, следующие 10% уже джинсы. На самом деле он заказал столько кроссовок, чтобы примерить и выбрать только одни. Что будет, если это не поправить? Модель переобучится: она прекрасно предсказывает, что дальше будут белые кроссовки, хотя по факту они были как единый action и по-хорошему модель должна была обучиться связывать кроссовки с джинсами в том же стиле. Как итог: возникает эффект, с которым я иногда сталкиваюсь на маркетплейсах. Например, я получила сотню розовых юбок в ленте рекомендаций, потому что перед этим искала наряд на свадьбу подруги и взяла сразу пять штук на примерку (хотя на самом деле мне это больше не нужно). То есть деление 8:1:1 было некорректное. Либо нужно менять деление так, чтобы джинсы попали в валид, либо вообще сжать кроссовки в единое действие и расширить окно наблюдений, либо ещё что-то. 

В этом примере единый action мог бы объясняться тем, что таймстемпы оказались в пределах одних суток, но может быть и что-то менее очевидное. Как это отловить?

<details markdown="1">
  <summary>Аналогия из линейной регрессии</summary>
  Вот в линейной регрессии, допустим, мы перед построением модели сначала выводим матрицу корреляций. До этого теста я могу вообще не догадываться, что какие-то величины были линейно связаны, даже если я хорошо знаю свой домен. После того, как я увидела матрицу, я уже могу догадаться, что прошло мимо моего внимания. Дальше мне решать, что с этим делать.

  
  То есть в данном случае мой claim: величины не коррелируют. Это пример чисто математического теста на корректность claim, который не зависит от домена – не важно, чем я занимаюсь (хоть титаник, хоть квартиры). Хотелось бы нечто аналогичное для темпоральных данных.
</details>

Более того, есть следующий уровень проблемы. На valid мы видим отличные метрики. Казалось бы, test должен отловить переобучение, но **test в данном случае очень близко по таймингу! Мы предсказываем ближайшее будущее, а прод будет работать долго!** Даже если мы заметим, что события слишком однородны, и попытаемся это пофиксить, мы всё равно ограничены в наблюдениях, модель переобучена под окошко. Это не leakage, а просто оверфит. Хотелось бы измерить и это (чтобы принимать решение, выпускать ли модель в прод, нужно ли нам больше данных или окошко уже достаточно репрезентативно). 

> Как формально описать, что за claim мы делаем, когда решаемся сделать такой сплит? Во-первых, почему 8:1:1? Во-вторых, почему данных из окошка достаточно?
>
> Есть ли универсальный способ диагностировать, что сплит плохой?
>
> Честно говоря, я не знаю, и меня очень пугает, что я закрыла ML1 и ML2 в ШАДе на пятёрки, но ответ мне не очевиден. Я
поспрашивала LLM. 

## Определения
Когда вы решаете сделать хронологический сплит $8:1:1$ (или любой другой), вы делаете утверждение о свойствах процесса $\{Z_t\}$

Вы утверждаете, что совместное распределение для лага $k = 1$ (шаг предсказания) не зависит от абсолютного времени $t$:

$$
P(Z_{t+1} \mid Z_t, Z_{t-1}, \dots) = P(Z_{s+1} \mid Z_s, Z_{s-1}, \dots), \quad \forall t, s \in [0, T]
$$

Грубо говоря, сплит $8:1:1$ взято с потолка, это не следствие теоремы и не закон природы. С тем же успехом можно было взять $6:2:2$. Claim состоит в том, что можно брать любой сплит как нам нравится.

## Тесты
Хотелось бы видеть какой-то warning: у вас модель предсказывает одно и то же, это точно то, что вы хотите? Да, это может быть ок, но можно пропустить мимо внимания серьёзную проблему. Дальше я уже принимаю решение: возможно, мне стоит увеличить окно, например.

Я почитала, оказалось, что это огромный айсберг для обсуждения:
* А что если были только белые кроссовки? Это warning!
* А что если были не белые кроссовки, а семантически близкие башмаки? И это тоже warning!

Остановимся только на нашем примере.

### Что такое стационарный временной ряд

Временной ряд \(X_1,\ldots,X_T\) называется **стационарным**, если его статистические свойства не зависят от абсолютного времени.

Для строгой (слабой) стационарности требуется:

$$
\mathbb E[X_t]=\mu,
$$

$$
\operatorname{Var}(X_t)=\sigma^2,
$$

и

$$
\operatorname{Cov}(X_t,X_{t+k})

=
\gamma(k),
$$

где \(\mu\), \(\sigma^2\) и \(\gamma(k)\) не зависят от \(t\), а только от лага \(k\).

То есть процесс может быть зависимым во времени — например, \(X_t\) может сильно зависеть от \(X_{t-1}\). Важно лишь, чтобы **характер этой зависимости не менялся со временем**.

Интуитивно:

$$
\boxed{
\text{стационарность}

=
\text{одинаковая статистическая структура в разные моменты времени}
}
$$

Например,

$$
5,7,6,5,8,6,7,5,\ldots
$$

может быть стационарным рядом: его уровень и характер колебаний примерно одинаковы на всём интервале.

А

$$
5,6,5,7,6,\quad
50,52,48,51,49,\ldots
$$

— уже нет: средний уровень процесса изменился.

В нашем случае это означает: если мы хотим использовать одно временное окно как представление будущего, мы предполагаем, что **процесс внутри него не проходит через систематические изменения режима**.

### ADF + KPSS
<details markdown="1">
  <summary>Описание</summary>

#### 1. Что проверяем

Если мы выбираем временное окно для обучения и оценки модели, мы неявно предполагаем, что **статистические свойства процесса внутри этого окна не меняются систематически со временем**.

Для временного ряда \(X_1,\ldots,X_T\) это можно формализовать как гипотезу стационарности:

$$
P(X_{t_1},\ldots,X_{t_k})

=
P(X_{t_1+h},\ldots,X_{t_k+h})
$$

для допустимых \(t_1,\ldots,t_k,h\).

На практике часто проверяют более слабое условие:

$$
\mathbb E[X_t]=\mu,
\qquad
\operatorname{Var}(X_t)=\sigma^2,
\qquad
\operatorname{Cov}(X_t,X_{t+k})=\gamma_k,
$$

то есть среднее, дисперсия и автоковариация не зависят от абсолютного времени.

Для диагностики используют два теста с противоположными нулевыми гипотезами:

**ADF (Augmented Dickey–Fuller):**

$$
H_0:\text{ряд имеет unit root (нестационарен)}
$$

$$
H_1:\text{ряд стационарен}.
$$

**KPSS (Kwiatkowski–Phillips–Schmidt–Shin):**

$$
H_0:\text{ряд стационарен}
$$

$$
H_1:\text{ряд нестационарен}.
$$

Поэтому совместный результат интерпретируется так:

| ADF                  | KPSS                 | Интерпретация                           |
| -------------------- | -------------------- | --------------------------------------- |
| отвергаем \(H_0\)    | не отвергаем \(H_0\) | есть основания считать ряд стационарным |
| не отвергаем \(H_0\) | отвергаем \(H_0\)    | есть свидетельства нестационарности     |
| отвергаем \(H_0\)    | отвергаем \(H_0\)    | результат неоднозначен                  |
| не отвергаем \(H_0\) | не отвергаем \(H_0\) | недостаточно свидетельств для вывода    |

Важно: **«не отвергли \(H_0\)» не означает, что \(H_0\) доказана.**

---

#### 2. Как это сделать

ADF и KPSS применяются к **числовому временному ряду**. Поэтому сначала нужно выбрать измеримую характеристику событий:

$$
X_t=g(Z_t).
$$

Например:

* цена покупки;
* число действий пользователя за час;
* cosine similarity между соседними товарами;
* среднее значение embedding-признака;
* какой-либо scalar score модели.

После этого:

1. сортируем значения по времени;
2. получаем ряд \(X_1,\ldots,X_T\);
3. запускаем ADF и KPSS;
4. смотрим на статистику теста и \(p\)-value.

Например:

```python
from statsmodels.tsa.stattools import adfuller, kpss

adf_p = adfuller(x)[1]
kpss_p = kpss(x, regression="c", nlags="auto")[1]

print("ADF p-value:", adf_p)
print("KPSS p-value:", kpss_p)
```

Если

$$
p_{\mathrm{ADF}} < 0.05
$$

и

$$
p_{\mathrm{KPSS}} > 0.05,
$$

то данные согласуются со стационарностью.

Если наоборот,

$$
p_{\mathrm{ADF}} > 0.05,
\qquad
p_{\mathrm{KPSS}} < 0.05,
$$

это warning о нестационарности.

---

#### 3. Что происходит в примере с кроссовками

Пусть вместо самих товаров мы рассматриваем числовой признак

$$
X_t =
\operatorname{sim}(item_t,item_{t-1}).
$$

Первые 80% истории состоят из нескольких очень похожих действий с белыми кроссовками:

$$
X_t\approx1.
$$

Затем пользователь переходит к джинсам:

$$
X_t\approx0.2.
$$

Получается временной ряд примерно такого вида:

$$
\underbrace{1,1,1,1,1,1,\ldots}_{\text{кроссовки}}
\rightarrow
\underbrace{0.2,0.2,0.2,\ldots}_{\text{джинсы}}.
$$

Средний уровень ряда изменился во времени:

$$
\mathbb E[X_t]\neq\text{const}.
$$

Следовательно, на всём этом интервале есть основания подозревать нестационарность. ADF и KPSS могут обнаружить это как статистически значимое изменение структуры ряда.

Но теперь рассмотрим другой случай:

$$
1,1,1,1,1,\ldots,1.
$$

ADF/KPSS могут не дать warning о нестационарности. И это **не означает, что наше окно хорошее**: мы просто наблюдали один режим поведения.

Поэтому ADF + KPSS отвечают на более узкий вопрос:

> **«Есть ли внутри выбранного временного окна статистические свидетельства изменения свойств процесса?»**

Если ответ «да», это warning, что одно окно может смешивать разные режимы.

Если ответ «нет», это лишь означает, что такой проблемы тест не обнаружил.

</details>

### KS-тест

### ACF


# Variance and randomness

# Statistical significance

# Effect size / practical significance

# Multiple comparisons


Пусть мы одновременно проверяем m нулевых гипотез:

$$
H_1,\ldots,H_m.
$$

Для каждой гипотезы используем уровень значимости \(\alpha\):

$$
P(\text{reject }H_i\mid H_i\text{ true})\leq\alpha.
$$

Если проверяется **одна** гипотеза, это контролирует вероятность ошибки I рода.

Но при множественном тестировании нас интересует вероятность сделать **хотя бы одну** ошибку I рода среди всех \(m\) проверок:

$$
\mathrm{FWER}
=
P\left(
\bigcup_{i=1}^{m}
\{\text{reject }H_i\mid H_i\text{ true}\}
\right).
$$

Это называется **family-wise error rate (FWER)**.

Если все \(m\) нулевых гипотез истинны и тесты независимы, то

$$
\mathrm{FWER}
=
1-(1-\alpha)^m.
$$

Например, при

$$
\alpha=0.05,\qquad m=20
$$

получаем

$$
\mathrm{FWER}
=
1-0.95^{20}
\approx 0.642.
$$

То есть даже если **все 20 гипотез ложны в смысле эффекта отсутствия**, вероятность получить хотя бы один `p < 0.05` составляет около **64%**.

# Experimental design under constraints

How to know whether an experiment is feasible before running it

Pilot experiments

Choosing the minimum experiment that can distinguish hypotheses

Resource / compute budget

# LLM as a judge (OpenAI)

# Changing Anything Changes Everything (NeurIPS, 2015)

# 6 Lessons Learned at Booking.com

# Demystifying evals for AI agents (Anthropic)
